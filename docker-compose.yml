services:
  credchain-python:
    build:
      context: .
    image: arfanxn/credchain-python:latest
    container_name: credchain-python
    env_file:
      - .env.docker
    ports:
      - "127.0.0.1:8081:8081"
    networks:
      - backend

networks:
  backend:
    external: true
